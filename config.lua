local config = {
  physicsDebug = true,
  fullscreen = false,

  borderColor = { 9 / 255, 18 / 255, 26 / 255 },
  windowWidth = 800,
  windowHeight = 600,

  gridScale = 16,
  wallWidth = 2,
  doorWidth = 3,


  enemyChaseDistance = {
    max = 250,
    min = 150,
  },
  enemyPathInterval = 0.5,
}

return config
